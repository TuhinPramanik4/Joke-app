<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Creators</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black p-6">
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    <% creators.forEach(creator => { %>
      <div class="bg-white shadow-md rounded-lg p-2">
        <img src="<%= creator.pictureUrl %>" alt="<%= creator.name %>" class="w-full h-40 object-cover mb-1 rounded">
        <h2 class="text-xl font-bold"><%= creator.name %></h2>
        <a href="<%= creator.youtubeLink %>" target="_blank" class="text-blue-500">YouTube Channel</a>
        <p class="text-gray-700 mt-2">Votes: <%= creator.votes %></p>

        <form action="/creators/<%= creator._id %>/upvote" method="POST" class="inline-block mt-4">
          <button class="bg-green-500 text-white px-4 py-2 rounded">Upvote</button>
        </form>

        <form action="/creators/<%= creator._id %>/downvote" method="POST" class="inline-block mt-4">
          <button class="bg-red-500 text-white px-4 py-2 rounded">Downvote</button>
        </form>
      </div>
    <% }) %>
  </div>
</body>
</html>
